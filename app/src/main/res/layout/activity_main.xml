<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/blue_primary">

    <include layout="@layout/toolbar_base" />

    <View
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:background="@drawable/toolbar_separator"/>

    <com.ohad.babysitter.thirdparty.PagerSlidingTabStrip
        android:id="@+id/mTabs"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/white"
        app:pstsActivateTextColor="@color/toolbar"
        app:pstsDeactivateTextColor="@color/toolbar"
        app:pstsIndicatorColor="@color/toolbar"
        app:pstsDividerColor="@color/toolbar"
        app:pstsIndicatorHeight="2dp"
        app:pstsShouldExpand="true"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/mPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>
